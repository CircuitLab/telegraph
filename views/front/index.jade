extends ../layout

block content
  #front
    .content
      #hand
        img#handImg(src='/img/hand@2x.png')
      #title
        テバタイプ
      a#startButton(href='start')
        img#startBtnImg(src='/img/startBtn@2x.png')
  #input
    .content
      #instruct
        メッセージを入力してください。
      #restriction
       （10文字まで）
      form(method='post', action='message')
        #inputArea
          input#inputText(type='text', name='message', maxlength='10')
        #okBtn
          input#okImg(src='/img/okBtn@2x.png', type='image')
  #semaphore 
    .content
      -letters.forEach(function(letter){
        div.letter(value=letter, id=letter) 
          =letter
      -});
  #end
    .content
      #thanks.thanks
        ありがとうございました
      #guidance
        以下のアドレスから画像を確認出来ます。
      .generatedLink
        #url
          http://exsample.com
        .qr
          img#qrImg(src='')

      .control
        a#shareButton(href='share')
          img#shareBtnImg(src='/img/shareBtn@2x.png')
        a#endButton(href='end')
          img#endBtnImg(src='/img/endBtn@2x.png')



